# Port du serveur mock backend (par défaut: 4000)
PORT=4000

# Environment (development|production)
NODE_ENV=development

# Chemin vers la base de données JSON
DB_PATH=./data/trips.json

# Répertoire des fichiers uploadés
UPLOADS_DIR=./uploads
